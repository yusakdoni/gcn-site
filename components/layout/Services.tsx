import Image from "next/image";
import Link from "next/link";
import { Container } from "@/components/ui/Container";
import { SectionHeader } from "@/components/ui/SectionHeader";
import { SERVICES } from "@/lib/data/services";
import { T } from "@/components/i18n/T";
import { SERVICES_ID } from "@/lib/i18n/data-id";
export function Services(){return <section className="py-24 md:py-32 bg-offwhite"><Container><div className="flex flex-col md:flex-row md:items-end md:justify-between gap-6"><SectionHeader eyebrow={<T en="What We Do" id="Yang Kami Kerjakan"/>} title={<T en="Five practical service lines" id="Lima lini layanan yang praktis"/>}/><Link href="/services" className="text-nav text-corporate hover:text-navy underline underline-offset-4 whitespace-nowrap"><T en="View all services" id="Lihat semua layanan"/></Link></div><div className="grid md:grid-cols-2 lg:grid-cols-3 gap-6 mt-16">{SERVICES.map((s,i)=>{const tr=SERVICES_ID[s.slug];return <Link key={s.slug} href={`/services/${s.slug}`} className="group bg-white border border-mist overflow-hidden hover:border-navy transition-colors"><div className="relative h-56"><Image src={s.image.src} alt={s.image.alt} fill sizes="(max-width: 768px) 100vw, (max-width: 1024px) 50vw, 33vw" className="object-cover transition-transform duration-700 group-hover:scale-105"/><span className="absolute top-4 left-4 bg-white px-3 py-2 text-[11px] uppercase tracking-widest text-deep-blue">{String(i+1).padStart(2,"0")}</span></div><div className="p-7"><h3 className="text-h3 text-lg group-hover:text-corporate"><T en={s.title} id={tr?.title||s.title}/></h3><p className="text-[15px] text-navy/70 leading-relaxed mt-4"><T en={s.summary} id={tr?.summary||s.summary}/></p><span className="text-nav text-corporate mt-5 inline-block"><T en="Explore scope →" id="Lihat cakupan →"/></span></div></Link>})}</div></Container></section>}
